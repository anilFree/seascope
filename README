SeaScope is a GUI frontend for cscope.

* Running the application

	cd src
	python SeaScope.py

* Generating source distribution package
	python setup.py sdist

* Generating RPM distribution package
	python setup.py bdist_rpm

